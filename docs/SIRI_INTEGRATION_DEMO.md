# 🚀 Siri 捷徑快速設定指南 (試用者專用)

> [!TIP]
> **本文件專為使用 [展示網頁](https://voice-notion.jacktoholiday.uk/demo) 的使用者設計。**
> 只要您可以從網頁端複製到「封裝後的設定 JSON」，請按照以下步驟快速將其匯入 iOS 捷徑。

---

## 步驟 1：網頁端準備
1. 在展示網頁輸入您的 Gemini API Key 與 Notion Token。
2. 點擊「封裝捷徑設定」按鈕。
3. **複製**產生的那一串包含大括號 `{...}` 的 JSON 代碼。

## 步驟 2：iOS 捷徑建立
1. 開啟 iPhone 上的「**捷徑 (Shortcuts)**」App，點擊右上角「+」建立新捷徑。
2. **加入動作**：搜尋「**錄音 (Record Audio)**」。
3. **加入動作**：搜尋「**文字 (Text)**」。
   - 直接將從網頁複製的 **JSON 完整貼入**此文字框。
4. **加入動作**：搜尋「**從輸入項目取得辭典 (Get Dictionary from Input)**」。
   - 捷徑會自動將上方的「文字」作為輸入，並解析為辭典格式。
5. **加入動作**：搜尋「**取得 URL 內容 (Get Contents of URL)**」。
   - **URL**：輸入展示網頁中提供的 API 端點。
   - 點擊展開箭頭 (顯示更多)：
     - **方法 (Method)**：選擇 `POST`。
     - **標題 (Headers)**：點擊欄位，選擇剛才產生的變數「**辭典**」。
     - **要求回覆本文 (Request Body)**：選擇「**檔案 (File)**」。
     - **檔案 (File)**：選擇變數「**錄製的音訊**」。
6. **加入動作**：搜尋「**顯示通知**」並連結 `URL 內容`。
7. 將捷徑命名為「**記筆記**」或任何您喜歡的 Siri 喚醒詞。

---

## ⚡ 喚醒方式
對著 iPhone 說：「**嘿 Siri，記筆記**」，錄音完成後您的筆記便會自動飛往 Notion！
